📊 Data Analyzer Pro

Data Analyzer Pro is a lightweight, no-code data analysis application that allows users to upload multiple datasets, merge them automatically, explore data, apply sorting and filtering, generate visual charts, clean data, and export results easily. Built for anyone who wants fast insights without writing code.

🚀 Features

- Upload & combine multiple files
- Sort, filter & search across data
- Pivot tables, grouping, aggregation
- Interactive charts (bar, line, pie, etc.)
- Data cleaning (duplicates, missing values, outliers)
- Export to Excel / CSV / PDF
- Basic authentication & preset saving

🛠 Tech Stack

- Python
- Streamlit
- Pandas
- Plotly

📦 Run Locally
pip install -r requirements.txt
streamlit run app.py
